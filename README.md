Installation Guide:
# KitchIndex Frontend Setup (React + Vite)

## Prerequisites
Make sure you have these installed on your computer:

- Visual Studio Code (VS Code)
- Node.js

## Required npm packages to install in terminal before running in VS Code

npm install framer-motion lucide-react

## Run locally

npm run dev

Frontend expects backend running at: http://localhost:5000
